---
title: "Workspace and team settings"
sidebarTitle: "Team and workspace"
description: "Rename your workspace, set model-wide time defaults and Last close, and look up members and groups."
---

Your workspace has a few settings that apply model-wide, rather than to any single Variable or Table Block. Ask Ari to change most of these directly.

## Rename the workspace

Ask Ari to rename your workspace. Renaming regenerates the workspace's URL slug, so any link built from the old slug changes along with it.

## Time defaults

Ask Ari to set the model's default time settings:

- **Granularity** — the default roll-up grain for new tables, usually month.
- **Range** — the default start and end date a new table shows.

These are defaults for new Table Blocks; an existing table's own window (see [Build and format a Table Block](/dashboards/build-and-format-tables)) isn't affected until you change it directly.

## Last close

**Last close** is the date your Model computes Actuals against — everything at or before it is Actuals, and everything after is Forecast. Ask Ari to set it after each period closes in your books. Several other workflows, like [building a Headcount page](/headcount/build-a-headcount-page), depend on Last close being set correctly first.

## Team directory and groups

Ask Ari to look up who is in the workspace, including current team members, their roles, and pending invitations. Ari can also list user groups and the group details your access allows you to see.

## Invite or remove workspace administrators

When you have the required access, Ari can invite someone as a workspace administrator, promote an existing teammate to administrator, or remove a person from the workspace.

An administrator has broad workspace access. If your goal is to share a report without granting administrator access, use the Page's **Share** control instead.

Ari cannot invite someone with a lower role, create or edit groups, change Page-specific access, or create a share link on your behalf.

## Related

- [Billing and plan](/workspace-settings/billing)
- [Personal theme](/workspace-settings/personal-theme)
- [Actuals, Forecast, and formula ranges](/formulas/core-concepts/actuals-forecast-and-formula-ranges)
- [Share a Page and choose its Scenario](/dashboards/share-pages-and-scenarios)
