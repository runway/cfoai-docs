---
title: "Browse the integrations directory"
sidebarTitle: "Integrations directory"
description: "Connect the accounting, HR, spreadsheet, and database sources cfo.ai can pull Actuals from, and browse the connectors available to Ari."
---

cfo.ai connects to the systems where your financial and operational data already lives, so your Model stays current without manual entry.

## Standard and premium integrations

Your plan can classify connected sources as standard or premium:

- **Standard**: QuickBooks Online, Xero, Rippling, Google Sheets, and many more.
- **Premium**: NetSuite SuiteAnalytics and all database connections.

File upload is not a billable integration. Check your workspace's Usage or billing settings for the current limits and classification that apply to your plan.

## File upload

**CSV / Raw File Upload** is a manual option for bringing in a raw file directly. Use it when a live connection is not set up yet, or for a one-off or specialized report a system does not sync automatically. File upload is not a billable integration.

An uploaded file is tracked as its own connection, using its filename until you rename it, so ask Ari by that name if you need to check on it later. An uploaded file never refreshes on its own. To bring in newer data, upload the file again; the new upload replaces the data from the previous one.

After upload, ask Ari whether the file has finished loading. Ingestion runs in the background, and a Model that ends up with fewer rows than the source file is not necessarily a problem: a query that intentionally scopes the data down looks the same from the outside as one that is missing rows, so ask Ari to explain what the load produced before treating a smaller row count as an error.

This is for bringing a file's data into your Model as Actuals. To attach a file to an Ari chat message instead, for Ari to read a screenshot, PDF, or spreadsheet in the moment, see [Attach files to a chat message](/workspace-settings/file-attachments-in-chat).

## Accounting and ERP

- **QuickBooks Online** (Standard)
- **Xero** (Standard)
- **Rillet** — connects with a Rillet API key; a workspace holds one Rillet connection.
- **NetSuite SuiteAnalytics** (via Fivetran, Premium)

Use an accounting or ERP integration when you want Actuals to update automatically each time your books close. QuickBooks Online connections load the profit and loss statement and the balance sheet; Ari builds cash flow from those.

cfo.ai checks that NetSuite shares the tables the income statement and balance sheet need when the connection first finishes setup, and again before every sync, whether the nightly refresh or one you ask Ari for. If a table is missing, the connection's row on the **Integrations** tab of the connection manager carries an **Access missing** badge, and its warning reads: **NetSuite isn't sharing some tables cfo.ai needs for your financial statements. Have your NetSuite admin create an access token for the Data Warehouse Integrator role, then reconnect NetSuite with that token. Changing only the role ID won't work. Missing tables: …**

A NetSuite access token belongs to one role, so entering a different role ID in the connection form changes nothing; the connection needs a token issued for the Data Warehouse Integrator role. Once that token is in place, the next sync asks NetSuite for its table list again, syncs the tables it finds, and clears the warning in the same run. While the warning stands, a sync that brings back no statement rows reports the missing access, with the next step **Grant access to the missing source tables to continue**, rather than an empty source. Ari sees the same warning when it inspects the connection. It can start a sync to re-check, but it does not rewrite the connection's saved queries to work around the gap.

## HR

- **Rippling** (Standard)

Connect Rippling to bring headcount, compensation, and other people data into your Model alongside your financials. cfo.ai's catalog also lists a much longer roster of HR and payroll systems available to connect (via Merge), including BambooHR, Gusto, Workday, ADP Workforce Now, Paychex, Paycor, Justworks, TriNet, UKG Pro, Deel, Personio, Hibob, and dozens more. If your HR system is not Rippling, ask Ari to check whether it is in the catalog before assuming you need a file upload or manual entry instead.

A new Rippling connection syncs the seven tables cfo.ai's daily employee roster reads: workers, users, worker details, departments, teams, team membership, and work locations. There is no table picker for Rippling. Rippling connections made earlier keep the tables they already sync. A numeric roster column such as pay rate reads, in each period, each employee's latest value in that period; a period with no sync carries the previous value forward, and an explicit zero replaces it. Department and company totals add each employee's latest value once.

## CRM

- **HubSpot** (via Fivetran)
- **Salesforce** (via Fivetran)

Connect a CRM when Bookings, pipeline, or customer data needs to feed your Model directly, rather than being re-entered from a report export.

## Spreadsheets

- **Google Sheets** (via Fivetran, Standard)

Use a Google Sheets connection when a Variable is still maintained in a spreadsheet outside your other systems.

## Databases

- **PostgreSQL** (via Fivetran, Premium)
- **Amazon RDS for PostgreSQL** (via Fivetran, Premium)
- **Amazon Aurora PostgreSQL** (via Fivetran, Premium)
- **Google Cloud SQL for PostgreSQL** (via Fivetran, Premium)

Use a database connection when your data lives in a system of record that does not have a dedicated integration above, but is reachable through one of these PostgreSQL-compatible sources.

The connectors shown in your workspace's catalog can differ from this list depending on your plan and rollout. Ask Ari to check the catalog for a specific system.

## Connections for Ari

The integrations above bring data into your Model. Separately, **Settings → Connections** lets an authorized workspace administrator connect external tools and services for Ari to use directly. The **MCP** and **APIs** tabs show available connections and the services already connected for your workspace.

### MCP Connectors

Browsable by category:

- **Payments** — Stripe, PayPal, Square, Mercado Pago
- **Banking & Spend** — Mercury, Ramp, Brex, Expensify, Plaid
- **Accounting & Tax** — Avalara AvaTax, Avalara Returns, Numeric
- **HR & Payroll** — Gusto, Deel
- **Equity & Compliance** — Carta and Vanta
- **Market Data** — Alpha Vantage and Morningstar
- **CRM & Sales** — Intercom, Close, and Attio
- **Productivity & Docs** — Notion, Linear, Atlassian (Jira and Confluence), Airtable, monday.com
- **Data & Analytics** — Supabase and ThoughtSpot

If you do not see what you need, use **Add server** or the search bar to look for others.

For how to connect cfo.ai's own MCP server to Claude Code, see the [MCP Server](/integrations/mcp-server) page.

### APIs

A separate, direct list of API connections:

- Stripe
- Adyen (Transfers, Management, Balance Platform)
- Increase
- Synctera
- Mercury
- Brex
- Pleo
- Pennylane
- TaxJar
- Rippling
- CoinAPI
- Tradier
- Frankfurter — FX rates, no key needed
- U.S. Treasury Fiscal Data — no key needed

Use **Add API** or the search bar for anything not listed.

## How integrations stay current

Connected integrations refresh once a night, automatically. The refresh starts in the early morning UTC and finishes within a few hours, so your Actuals are fresh by the start of the US workday. Every integration follows the same schedule, whether it is QuickBooks Online, Xero, NetSuite, Google Sheets, or a database.

There is no manual refresh button today. If you need a number sooner than the next nightly run, ask Ari to refresh the connection:

> Refresh QuickBooks and tell me when the import has finished.

Ari starts the import in the background and can tell you when it completes. Before relying on a time-sensitive result, ask Ari when the source last finished syncing and whether it reported any problems.

When you first connect QuickBooks Online or Xero, cfo.ai loads Actuals from January 1 of the previous year through today. Ask Ari which accounting periods are available once the first load completes.

The nightly refresh and asking Ari to refresh a connection work the same on every active plan, including during a trial. Plans differ in how many connections you can hold, not in what a connection does once it is connected.

## API and MCP connections do not sync

The integrations above bring data into your Model on a schedule. API and MCP connections under **Settings → Connections** are different: they are tools Ari calls live when you ask a question. Nothing is stored or scheduled. If Ari writes a number from one of these calls into your Model, it lands as a plain value that stays put until you ask Ari to pull it again.

For example, Stripe is not a synced integration. Connect it as an MCP connector or an API connection, and Ari can look up a balance or a payout when you ask, then write the figure into a Table Block as an editable value.

## Something look off?

<Card title="Ask Ari" icon="sparkles" href="https://app.cfo.ai">
  Ask Ari to check a specific number, account, or entity against its source.
</Card>
